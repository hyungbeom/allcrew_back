# PostgreSQL 사용 시 활성화: spring.profiles.active=postgres
spring.datasource.url=jdbc:postgresql://211.37.179.144:5433/allcrew
spring.datasource.username=postgres
spring.datasource.password=root1234
spring.datasource.driver-class-name=org.postgresql.Driver

# 운영/공유 DB는 수동으로 스키마 관리 (자동 실행 비활성화)
spring.sql.init.mode=never

spring.h2.console.enabled=false

# JWT / CORS
app.jwt.secret=allcrew-dev-secret-key-change-in-production
app.jwt.expiration-ms=86400000
app.cors.allowed-origins=http://localhost:3000
